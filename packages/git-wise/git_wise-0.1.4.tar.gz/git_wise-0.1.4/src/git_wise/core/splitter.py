# TODO feature: maybe we can use LLM to split the commits if the changes are too large🤔
def split_commits(changes, max_files_per_commit=5):
    """
    split the large changes into multiple smaller commits
    """
    pass
