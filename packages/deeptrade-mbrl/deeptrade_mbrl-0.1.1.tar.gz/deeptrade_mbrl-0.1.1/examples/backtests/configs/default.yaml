defaults:
  - _self_

log_dir: "./logs"

# Run Params
seed: 42
device: "cuda"
use_wandb: True

# Data Params
data_type: "random"
n_days: 10000
var: 0.1

# Model params
n_forecast: 10
num_members: 3
num_layers: 3
hid_size: 64

# Train params
num_epochs: 10000
# patience: 500