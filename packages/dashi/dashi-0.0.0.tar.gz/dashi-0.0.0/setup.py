from setuptools import setup, find_packages

setup(
    name='dashi',
    version='0.0.0',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
      )
