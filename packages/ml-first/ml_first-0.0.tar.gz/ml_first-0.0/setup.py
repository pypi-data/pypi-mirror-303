from setuptools import setup

setup(name='ml_first',
      version='0.0',
      description='ml_first',
      packages=['ml_first'],
      author_email='ilapronin0700@gmail.com',
      zip_safe=False)
