[project]
name = "ahmed-menu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "twine>=5.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
