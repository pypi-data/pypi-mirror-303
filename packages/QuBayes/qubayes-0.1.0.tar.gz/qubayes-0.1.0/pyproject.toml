[project]
name = "QuBayes"
version = "0.1.0"
description = "Python package for performing quantum Bayesian inference"
authors = [
    { name = "Zeji Li, Zi-ming Li" },
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "Apache-2.0"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
