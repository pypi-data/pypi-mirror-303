[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ranver-transaction-counter"
version = "0.01"
dependencies = [
  "pandas",
]
requires-python = ">=3.8"


[project.scripts]
transaction-sum = "transaction_counter:main"

