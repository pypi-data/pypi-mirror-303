[tool.poetry]
name = "caseChange"
version = "0.1.0"
description = "caseChange"
authors = ["s.nalawade <sonal.nalawade@ibm.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.12.2"
fastapi = "0.115.0"
uvicorn = "^0.22.0"

[build-system]
requires = ["poetry-core","setuptools","wheel"]
build-backend = "poetry.core.masonry.api"
