[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transactions-analysis-sk1"
version = "0.1.0"
description = "Пакет для анализа финансовых транзакций"
authors = [{ name = "Stanislav Kuleshov", email = "phtschmth171@gmail.com" }]
dependencies = ["pandas"]

[project.scripts]
transactions-analysis = "transactions_analysis_sk1.__main__:main"