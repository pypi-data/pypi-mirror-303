[build-system]
requires = [
    "setuptools>=42",  # Required for building the package
    "wheel",           # Required to build wheel distribution
    "numpy"            # Needed during build (for Cython extension)
]
build-backend = "setuptools.build_meta"
