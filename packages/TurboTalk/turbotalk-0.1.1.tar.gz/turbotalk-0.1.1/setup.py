from setuptools import setup

setup(
    name="TurboTalk",
    version="0.1.1",  # Increment the version number
    packages=["TurboTalk"],
    install_requires=[
        "g4f",
        "colorama",
    ],
)
