from setuptools import setup, find_packages

setup(
    name='rocket_seds_lab3_meriem_terki',
    version='0.3',  # Increment to a new version number
    packages=find_packages(),
    install_requires=[],  
    author='Meriem Terki',
    description='A package to simulate rockets and shuttles.',
    license='MIT',
)