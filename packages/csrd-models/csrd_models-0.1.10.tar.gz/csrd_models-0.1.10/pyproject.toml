[tool.poetry]
name = "csrd-models"
version = "0.1.10"
description = "models lib"
authors = ["aaron <aaron@frostmn.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pre-commit = "^3.8.0"
csrd-utils = "^0.1"

[[tool.poetry.source]]
name = "PyPI"
priority = "primary"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
