[project]
name = "notion_connect"
version = "0.1.1"
description = "A Python extension to help make Notion more useful for LLM applications"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
authors = [
    { name = "Ivan Leo", email = "ivanleomk@gmail.com" }
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
