[project]
name = "DESiModels"
version = "0.1.0"
description = "Ready-to-use discrete event simulation models."
authors = [
    { name = "galenseilis", email = "galen.seilis@seilis.ca" },
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "Apache-2.0"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
